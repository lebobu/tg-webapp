# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [ main, prod ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Write SSH key from secret
        run: |
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > key.pem
          chmod 600 key.pem

      - name: Deploy dispatch (server forced-command decides)
        run: |
          # передаём имя ветки в SSH_ORIGINAL_COMMAND
          ssh -o StrictHostKeyChecking=no -i key.pem root@217.114.1.224 "${GITHUB_REF_NAME}"
