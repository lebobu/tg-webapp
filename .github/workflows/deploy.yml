- name: Deploy via SSH
  run: |
    echo "${{ secrets.DEPLOY_BEGET }}" > key.pem
    chmod 600 key.pem
    ssh -i key.pem -o StrictHostKeyChecking=no root@217.114.1.224 '
      set -e
      git -C /root/tg-webapp-test-hook pull --ff-only